{# ------------------------------------------------------------------
   templates/_how_to_read_dataset.html.j2 — placeholder/stub.

   STUB STATUS: the dataset-card generator is specified in
   04_INTELLIGENCE/DATASET_CARD_BLUEPRINT.md (LOCKED 2026-05-23) but
   the implementation is **deferred to F0-T5** (Medallion + sharding
   WebDataset), when real datasets exist to describe.

   When F0-T5 is started, populate this file with the canonical
   "Come leggere il dataset card" text — analogous to
   _how_to_read.html.j2 ma con definizioni dataset-specifiche
   (Medallion Bronze/Silver/Gold, mic_config, flat-25, DNA-Trace,
   augmentation levels, license red/green markers).

   For now this file exists only so that lychee resolves the
   cross-reference from DATASET_CARD_BLUEPRINT.md without errors.
------------------------------------------------------------------ #}
<section id="section-9" class="how-to-read">
  <h2>§ 9 — Come leggere questo dataset card</h2>
  <p class="lead">
    <em>Sezione stub — contenuto da redigere in F0-T5 (Medallion +
    sharding WebDataset). Spec del contenuto in
    <code>04_INTELLIGENCE/DATASET_CARD_BLUEPRINT.md</code> § 9.</em>
  </p>
</section>
